#!/usr/bin/env php
<?php
// Update composer to the latest version ...
passthru('composer self-update --no-interaction');

$composerFlags = array();

// Install composer dependencies ...
$exitCode = 0;
passthru('composer install --dev --no-progress --no-interaction --ansi ' . join(" ", $composerFlags), $exitCode);
exit($exitCode);
